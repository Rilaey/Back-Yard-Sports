{{#each partials.cardContext}}
    <div calss ="card">
        <img src="{{card.url}}" alt="">
        <h2>{{card.name}}</h2>
        <p>{{card.sport}}</p>
        <p>{{card.city}}, {{card.state}}</p>
    </div>
{{/each}}